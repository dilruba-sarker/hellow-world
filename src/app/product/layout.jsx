import React from 'react';

const layout = ({children}) => {
    return (
        <div>
            <h1>layout pages</h1>
            {children}
        </div>
    );
};

export default layout;